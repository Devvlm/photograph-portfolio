{
  "name": "portfolio-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "hash-password": "node scripts/hash-password.js"
  },
  "devDependencies": {
    "wrangler": "^4.61.0"
  }
}
